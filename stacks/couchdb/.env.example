# ---- CouchDB bootstrap ----
# COUCHDB_USER (required) - Admin username used to bootstrap CouchDB. Format: string.
# Change these; do not commit real secrets.
COUCHDB_USER=<COUCHDB_USER>

# COUCHDB_PASSWORD (required) - Admin password used to bootstrap CouchDB. Format: secret.
# Change these; do not commit real secrets.
COUCHDB_PASSWORD=<PASSWORD>

# ---- CORS / security (optional) ----
# COUCHDB_CORS_ORIGINS (optional) - Allowed origins for local.d/10-cors.ini. Format: comma-separated list.
# Prefer explicit origins; wildcard (*) is for demo/dev only.
# Example (demo/dev only): COUCHDB_CORS_ORIGINS=http://localhost:3000,https://demo.local
# COUCHDB_CORS_ORIGINS=<CORS_ORIGINS>

# ---- Network / ports (optional) ----
# BIND_LOCALHOST (optional, runtime/override-only) - Bind HTTP to loopback on the host. Format: IP address.
# Example: BIND_LOCALHOST=127.0.0.1
# BIND_LOCALHOST=<BIND_LOCALHOST>

# HTTP_PORT (optional, runtime/override-only) - Host port published by runtime override. Format: port number.
# Example: HTTP_PORT=5984
# HTTP_PORT=<HTTP_PORT>
