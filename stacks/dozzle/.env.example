# homelab-stacks/stacks/dozzle/.env.example
# -------------------------------------------------------------------
# Dozzle environment template
# Copy this file to homelab-runtime/stacks/dozzle/.env
# and adjust values as needed.
# -------------------------------------------------------------------

# Timezone for container logs
TZ=<REGION/CITY>

# Docker socket path (used by docker-socket-proxy)
# Rootless example: /run/user/<UID>/docker.sock
DOCKER_SOCK=<DOCKER_SOCK>

# If you run Docker rootful, uncomment below:
# DOCKER_SOCK=/var/run/docker.sock

# Optional: local UI bind (recommended if you need host access)
# BIND_LOCALHOST=127.0.0.1
# HTTP_PORT=8081
