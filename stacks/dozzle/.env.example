# homelab-stacks/stacks/dozzle/.env.example
# -------------------------------------------------------------------
# Dozzle environment template
# Copy this file to homelab-runtime/stacks/dozzle/.env
# and adjust values as needed.
# -------------------------------------------------------------------

# ---- General ----
# TZ (optional) - IANA Region/City; unset = image default. Format: Region/City.
# Example: TZ=Etc/UTC
TZ=<REGION/CITY>

# ---- Docker socket (host) ----
# DOCKER_SOCK (required) - Host Docker socket path used by docker-socket-proxy. Format: absolute path.
# Rootful example: /var/run/docker.sock
# Rootless example: /run/user/<UID>/docker.sock (find UID with: id -u)
DOCKER_SOCK=<DOCKER_SOCK>

# ---- Network / ports (optional) ----
# BIND_LOCALHOST (optional, runtime/override-only) - Bind UI to loopback on the host. Format: IP address.
# Example: BIND_LOCALHOST=127.0.0.1
# BIND_LOCALHOST=<BIND_LOCALHOST>

# HTTP_PORT (optional, runtime/override-only) - Host port published by runtime override. Format: port number.
# Example: HTTP_PORT=8081
# HTTP_PORT=<HTTP_PORT>
