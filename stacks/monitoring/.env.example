# homelab-stacks/stacks/monitoring/.env.example
# -------------------------------------------------------------------
# Runtime environment for the Monitoring stack.
# Choose ROOTFUL or ROOTLESS below (see sections).
# -------------------------------------------------------------------

COMPOSE_PROJECT_NAME=monitoring
TZ=<REGION/CITY>
# Repo root used for bind mounts in compose.yaml
STACKS_DIR=/opt/homelab-stacks

# Grafana bootstrap (change these in your real .env)
GRAFANA_ADMIN_USER=<GRAFANA_ADMIN_USER>
GRAFANA_ADMIN_PASSWORD=<GRAFANA_ADMIN_PASSWORD>

# ---- Docker engine mode (informational) ----
# rootful = dockerd as root / rootless = dockerd as regular user
DOCKER_MODE=<DOCKER_MODE>

# ---- ROOTFUL (dockerd as root) ----
# Socket and containers directory from the host daemon:
DOCKER_SOCK=<ROOTFUL_DOCKER_SOCK>
DOCKER_CONTAINERS_DIR=<ROOTFUL_DOCKER_CONTAINERS_DIR>

# ---- ROOTLESS (dockerd as non-root) ----
# To use rootless, comment the ROOTFUL block above and un-comment both lines below.
# DOCKER_SOCK=<ROOTLESS_DOCKER_SOCK>
# DOCKER_CONTAINERS_DIR=<ROOTLESS_DOCKER_CONTAINERS_DIR>

# ---- SELinux ----
# On SELinux enforcing hosts, set <SELINUX_SUFFIX> for relabeling; otherwise leave empty.
SELINUX_SUFFIX=<SELINUX_SUFFIX>

# ---- Prometheus targets (comma-separated items; used in YAML flow lists) ----
# Examples:
# PROM_ALERTMANAGER_TARGETS=alertmanager:9093
# PROM_NEXTCLOUD_EXPORTER_TARGETS=nc-mysqld-exporter:9104,nc-redis-exporter:9121
PROM_ALERTMANAGER_TARGETS=<PROM_ALERTMANAGER_TARGETS>
PROM_SELF_TARGETS=<PROM_SELF_TARGETS>
PROM_BLACKBOX_TARGETS=<PROM_BLACKBOX_TARGETS>
PROM_NODE_EXPORTER_TARGETS=<PROM_NODE_EXPORTER_TARGETS>
PROM_LOKI_TARGETS=<PROM_LOKI_TARGETS>
PROM_PROMTAIL_TARGETS=<PROM_PROMTAIL_TARGETS>
PROM_UPTIME_KUMA_TARGETS=<PROM_UPTIME_KUMA_TARGETS>
PROM_CADVISOR_TARGETS=<PROM_CADVISOR_TARGETS>
PROM_NEXTCLOUD_EXPORTER_TARGETS=<PROM_NEXTCLOUD_EXPORTER_TARGETS>
PROM_ADGUARD_EXPORTER_TARGETS=<PROM_ADGUARD_EXPORTER_TARGETS>
PROM_ADGUARD_DNS_TARGETS=<PROM_ADGUARD_DNS_TARGETS>
PROM_CLOUDFLARED_TARGETS=<PROM_CLOUDFLARED_TARGETS>
PROM_COUCHDB_EXPORTER_TARGETS=<PROM_COUCHDB_EXPORTER_TARGETS>

# ---- Node filesystem mountpoints (used by alert rules) ----
ROOT_MOUNTPOINT=<ROOT_MOUNTPOINT>
ROOT_FSTYPE=<ROOT_FSTYPE>
BACKUP_MOUNTPOINT=<BACKUP_MOUNTPOINT>
BACKUP_FSTYPE=<BACKUP_FSTYPE>

# ---- Optional: Node exporter textfile collector (runtime override) ----
NODE_EXPORTER_TEXTFILE_DIR=<NODE_EXPORTER_TEXTFILE_DIR>

# ---- Optional: local bind for Grafana (only if you expose ports in your runtime override) ----
# BIND_LOCALHOST=<BIND_LOCALHOST>
# GRAFANA_PORT=<GRAFANA_PORT>
