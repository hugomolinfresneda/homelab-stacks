# homelab-stacks/stacks/cloudflared/cloudflared/config.yaml.example
# -------------------------------------------------------------------
# Cloudflared tunnel config template (no real values)
# Copy to homelab-runtime/stacks/cloudflared/cloudflared/config.yaml
# -------------------------------------------------------------------

tunnel: <TUNNEL_UUID>
credentials-file: <CLOUDFLARED_CRED_FILE>

ingress:
  - hostname: <SERVICE_SUBDOMAIN>.<YOUR_DOMAIN>
    service: http://<SERVICE_NAME>:<SERVICE_PORT>
  - service: http_status:404
