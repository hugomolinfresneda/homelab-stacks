# homelab-stacks/stacks/adguard-home/.env.example

# ---- General ----
# TZ (optional) - IANA Region/City; unset = image default. Format: Region/City.
# Example: TZ=Etc/UTC
TZ=<REGION/CITY>

# ---- Network / ports (optional) ----
# WEB_PORT (optional, runtime/override-only) - Host UI port published by runtime override. Format: port number.
# Example: WEB_PORT=3000
WEB_PORT=<WEB_PORT>

# DNS_PORT (optional, runtime/override-only) - Host DNS port published by runtime override. Format: port number.
# Note: 53 is privileged and may require rootful or extra capabilities; rootless setups often cannot bind it.
# Example (privileged): DNS_PORT=53
# Example (non-privileged; clients must use a custom port): DNS_PORT=5353
DNS_PORT=<DNS_PORT>

# BIND_LOCALHOST (optional, runtime/override-only) - Bind UI only to loopback on the host. Format: IP address.
# Example: BIND_LOCALHOST=127.0.0.1
# BIND_LOCALHOST=<BIND_LOCALHOST>

# ---- Exporter / metrics (optional) ----
# These variables are only needed when running the AdGuard exporter.
# ADGUARD_PROTOCOL (optional) - API protocol. Allowed values: http|https.
# Example: ADGUARD_PROTOCOL=http
ADGUARD_PROTOCOL=<ADGUARD_PROTOCOL>

# ADGUARD_HOSTNAME (optional) - API hostname or service name. Format: hostname.
# Example: ADGUARD_HOSTNAME=adguard-home
ADGUARD_HOSTNAME=<ADGUARD_HOSTNAME>

# ADGUARD_PORT (optional) - API port. Format: port number.
# Example: ADGUARD_PORT=3000
ADGUARD_PORT=<ADGUARD_PORT>

# ADGUARD_USER (optional) - API username for the exporter. Format: string.
ADGUARD_USER=<ADGUARD_USER>

# ADGUARD_PASS (optional) - API password for the exporter. Format: secret.
# Example: ADGUARD_PASS=<PASSWORD>
ADGUARD_PASS=<PASSWORD>

# ADGUARD_EXPORTER_PORT (optional) - Exporter listen port. Format: port number.
# Example: ADGUARD_EXPORTER_PORT=9617
ADGUARD_EXPORTER_PORT=<ADGUARD_EXPORTER_PORT>

# ADGUARD_SCRAPE_INTERVAL (optional) - Exporter scrape interval. Format: duration (e.g., 10s, 1m).
# Example: ADGUARD_SCRAPE_INTERVAL=30s
ADGUARD_SCRAPE_INTERVAL=<ADGUARD_SCRAPE_INTERVAL>
