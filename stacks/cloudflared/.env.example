# homelab-stacks/stacks/cloudflared/.env.example
# -------------------------------------------------------------------
# Cloudflare Tunnel environment template
# Copy to homelab-runtime/stacks/cloudflared/.env and adjust as needed.
# -------------------------------------------------------------------

TZ=<REGION/CITY>

# UID/GID the container should run as.
CLOUDFLARED_UID=65532
CLOUDFLARED_GID=65532

# Path to the JSON credential file on the host; mounted 1:1 into the container.
CLOUDFLARED_CRED_FILE=<PATH_TO_CREDENTIAL_JSON>
