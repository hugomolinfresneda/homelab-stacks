# homelab-stacks/stacks/uptime-kuma/.env.example
# -------------------------------------------------------------------
# Uptime Kuma environment template
# Copy this file to homelab-runtime/stacks/uptime-kuma/.env
# and adjust values as needed.
# -------------------------------------------------------------------

# Timezone for container logs
TZ=<REGION/CITY>

# Optional: bind locally if needed (for development)
# BIND_LOCALHOST=<BIND_LOCALHOST>
# HTTP_PORT=<HTTP_PORT>
