; Auth secret for CouchDB HTTP cookies
; Generate once per environment:
;   openssl rand -hex 32
; Runtime path (not versioned, mode 600):
;   Example (set your runtime root): <ABS_PATH_TO_RUNTIME_ROOT>/stacks/couchdb/local.d/30-auth.ini

[chttpd_auth]
secret = <HEX_64>
