# stacks/monitoring/.env.demo — demo env (template)
# -------------------------------------------------
# Corporate-friendly, portable. Rootless by default.

TZ=Europe/Madrid
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# ----- Docker engine mode (informational) -----
# rootful = dockerd as root / rootless = dockerd as regular user
DOCKER_MODE=rootless

# ----- ROOTFUL (dockerd as root) -----
# Uncomment if you run the system daemon as root:
# DOCKER_SOCK=/var/run/docker.sock
# DOCKER_CONTAINERS_DIR=/var/lib/docker/containers

# ----- ROOTLESS (dockerd as user) -----
# Adjust UID/USER if needed:
DOCKER_SOCK=/run/user/1000/docker.sock
DOCKER_CONTAINERS_DIR=/home/hugo/.local/share/docker/containers

# ----- SELinux -----
# On Fedora/RHEL enforcing use :Z; on Debian/Ubuntu/Proxmox leave empty.
SELINUX_SUFFIX=

# ----- Optional local bind for Grafana (demo only) -----
BIND_LOCALHOST=127.0.0.1
GRAFANA_PORT=3003
