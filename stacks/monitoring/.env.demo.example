# homelab-stacks/stacks/monitoring/.env.demo.example
# -------------------------------------------------------------------
# Demo environment template (no secrets)
#
# Defaults:
# - Rootful Docker (typical Debian/Ubuntu)
# - No SELinux relabeling
# - Project-scoped resource names via COMPOSE_PROJECT_NAME
#
# Override when needed:
# - Rootless Docker (socket + containers dir)
# - SELinux enforcing (SELINUX_SUFFIX=,z)
# -------------------------------------------------------------------

TZ=Europe/Madrid

# Grafana (demo)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# ----- Compose project / naming -----
# This demo is project-scoped. The actual project name is driven by Makefile.demo:
#
#   make -f stacks/monitoring/Makefile.demo demo-up DEMO_PROJECT=demo-test
#
# If DEMO_PROJECT is not provided, Makefile.demo defaults to "mon-demo".
# Keep COMPOSE_PROJECT_NAME aligned with that default (or override it deliberately).
COMPOSE_PROJECT_NAME=mon-demo

# ----- Docker engine mode (informational) -----
DOCKER_MODE=rootful

# ----- Docker paths (ROOTFUL defaults) -----
DOCKER_SOCK=/var/run/docker.sock
DOCKER_CONTAINERS_DIR=/var/lib/docker/containers

# ----- ROOTLESS override (uncomment + adjust) -----
# DOCKER_MODE=rootless
# DOCKER_SOCK=/run/user/1000/docker.sock
# DOCKER_CONTAINERS_DIR=/home/<user>/.local/share/docker/containers

# ----- SELinux -----
# On Fedora/RHEL enforcing use ",z" (shared relabel) or ",Z" (private).
# On Debian/Ubuntu leave empty.
SELINUX_SUFFIX=

# ----- Optional local bind for Grafana (demo only) -----
BIND_LOCALHOST=127.0.0.1
GRAFANA_PORT=3003
